# don't ever lint node_modules
node_modules
# don't lint build output (make sure it's set to your correct build folder name)
dist
# don't lint nyc coverage output
coverage
# build
build
# samples
samples
# storybook
.storybook
#circleci
.circleci
#cypress
cypress/fixtures
cypress/plugins
cypress/support
//TODO: remove serviceWorker from ignore
# service worker from create react app will be ignore until PWA is setup
serviceWorker.ts